Assuming you have docker desktop installed, this should run normally by using
the following command:

```bash
docker compose up -d
```

This allows the tables articles and activity table to be created
